"""
features.py

Phase 2 module — reusable feature engineering functions, extracted from
05_feature_engineering.ipynb once the feature set is finalized. Having these
as functions (rather than notebook-only cells) allows the same transformations
to be applied consistently to new/incoming data during model inference.

Not yet implemented.
"""
